gh issue create --title "Scalability" --body "The solution must be able to handle a large number of devices and a high volume of data without performance degradation."

gh issue create --title "Security: code should be security scanned" --body "All committed code should security scanned"

gh issue create --title "Protect main branch from non-reviewed changes" --body "Main branch is protected from non reviewed changes"

gh issue create --title "All unit tests must pass" --body "All unit tests must pass"

gh issue create --title "End to end tests must pass before merging" --body "End to end tests must pass before merging to main"

gh issue create --title "Configure Dev, Test, and Prod environments" --body "Dev*, test, prod environments"